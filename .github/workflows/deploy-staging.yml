name: deploy-staging

on:
  push:
    branches: [develop]

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploy to staging"
      - run: echo "Run migrations"
      - run: echo "Smoke test /api/health"
      - run: echo "Notify Slack"
