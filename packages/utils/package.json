{
  "name": "@repo/utils",
  "version": "0.1.0",
  "description": "Shared utility functions",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@repo/tsconfig": "*",
    "typescript": "^5.2.0"
  }
}